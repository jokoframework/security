<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.1</version>
        <relativePath/>
    </parent>

    <groupId>io.github.jokoframework</groupId>
    <artifactId>joko-security-parent</artifactId>

    <version>2.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <modules>
        <module>joko-security-core</module>
        <module>joko-security-storage-postgres</module>
        <module>joko-security-web</module>
        <module>joko-security-autoconfigure</module>
        <module>joko-security-starter</module>
    </modules>

    <properties>
        <java.version>17</java.version>
        <commons-lang3.version>3.14.0</commons-lang3.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.source>17</maven.compiler.source>
        <maven.compiler.target>17</maven.compiler.target>
        <spring-boot.version>3.3.1</spring-boot.version>
        <commons-beanutils.version>1.9.4</commons-beanutils.version>
        <commons-io.version>2.15.1</commons-io.version>
        <httpclient.version>5.3.1</httpclient.version>
        <jjwt.version>0.12.6</jjwt.version>
        <spring-security-test.version>5.8.10</spring-security-test.version>
        <commons-collections4.version>4.4</commons-collections4.version>
        <dependency-check.version>9.0.9</dependency-check.version>
        <postgresql.version>42.7.4</postgresql.version>
        <guava.version>33.0.0-jre</guava.version>
        <jackson-databind.version>2.18.2</jackson-databind.version>
        <jsonassert.version>1.5.1</jsonassert.version>
        <log4j-api.version>2.23.0</log4j-api.version>
        <log4j-slf4j.version>2.23.0</log4j-slf4j.version>
        <h2.version>2.2.224</h2.version>
        <ext.prop.dir>src/main/resources</ext.prop.dir>
        <junit.version>4.13.2</junit.version>
        <javase.version>2.6.0</javase.version>
        <snakeyaml.version>2.2</snakeyaml.version>
        <batik-dom.version>1.14</batik-dom.version>
        <batik-svggen.version>1.14</batik-svggen.version>
        <commons-codec.version>1.16.1</commons-codec.version>
        <testcontainers.version>1.19.3</testcontainers.version>
    </properties>

    <!-- El parent POM no tiene dependencias directas, solo las gestiona en dependencyManagement -->

    <dependencyManagement>
        <dependencies>
            <!-- Joko Security Modules -->
            <dependency>
                <groupId>io.github.jokoframework</groupId>
                <artifactId>joko-security-core</artifactId>
                <version>${project.version}</version>
            </dependency>

            <dependency>
                <groupId>io.github.jokoframework</groupId>
                <artifactId>joko-security-storage-postgres</artifactId>
                <version>${project.version}</version>
            </dependency>

            <dependency>
                <groupId>io.github.jokoframework</groupId>
                <artifactId>joko-security-autoconfigure</artifactId>
                <version>${project.version}</version>
            </dependency>

            <dependency>
                <groupId>io.github.jokoframework</groupId>
                <artifactId>joko-security-starter</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- security fix pending https://nvd.nist.gov/vuln/detail/CVE-2022-1471 -->
            <dependency>
                <groupId>org.yaml</groupId>
                <artifactId>snakeyaml</artifactId>
                <version>${snakeyaml.version}</version>
            </dependency>

        </dependencies>
    </dependencyManagement>    

    <!--Plugins -->
    <build>
        <plugins>
            <!--Establece la versión mínima de maven -->
            <plugin>
                <inherited>true</inherited>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <version>3.0.0-M3</version>
                <executions>
                    <execution>
                        <id>enforce-maven-3</id>
                        <goals>
                            <goal>enforce</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <requireMavenVersion>
                                    <version>3.1.0</version>
                                </requireMavenVersion>                
                            </rules>
                            <fail>true</fail>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
                <version>3.2.1</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>jar</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
                <version>3.2.0</version>
                <configuration>
                    <!-- Deshabilitar validación estricta de JavaDoc -->
                    <doclint>none</doclint>
                    <failOnError>false</failOnError>
                </configuration>
                <executions>
                    <execution>
                        <id>attach-javadocs</id>
                        <goals>
                            <goal>jar</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <!-- properties-maven-plugin removed: joko-security is now a library without application.properties -->

            <plugin>
                <groupId>org.owasp</groupId>
                <artifactId>dependency-check-maven</artifactId>
                <version>${dependency-check.version}</version>
                <configuration>
                    <failBuildOnCVSS>8</failBuildOnCVSS>
                    <suppressionFiles>dependency-check-suppressions.xml</suppressionFiles>
                    <formats>
                        <format>HTML</format>
                        <format>XML</format>
                    </formats>
                    <!-- No fallar si hay problemas al actualizar NVD -->
                    <failOnError>false</failOnError>
                    <!-- Usar caché local si no se puede actualizar -->
                    <cveValidForHours>48</cveValidForHours>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <!--Repositorios de Spring -->
    <repositories>
        <repository>
            <id>spring-releases</id>
            <url>https://repo.spring.io/libs-release</url>
        </repository>

        <!--Repo de jitpack -->
        <repository>
            <id>jitpack.io</id>
            <url>https://jitpack.io</url>
        </repository>

        <repository>
            <id>mulesoft</id>
            <url>https://repository.mulesoft.org/nexus/content/repositories/public</url>
        </repository>
    </repositories>

    <pluginRepositories>
        <pluginRepository>
            <id>spring-releases</id>
            <url>https://repo.spring.io/libs-release</url>
        </pluginRepository>
    </pluginRepositories>

    <profiles>
        <!-- Perfil para GitHub Packages (por defecto) -->
        <profile>
            <id>github</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <distributionManagement>
                <repository>
                    <id>github</id>
                    <name>GitHub Apache Maven Packages</name>
                    <url>https://maven.pkg.github.com/jokoframework/security</url>
                </repository>
            </distributionManagement>
        </profile>

        <!-- Perfil para Artifactory interno -->
        <profile>
            <id>artifactory</id>
            <distributionManagement>
                <repository>
                    <id>central</id>
                    <name>Artifactory Releases</name>
                    <url>https://artifactory.example.com/artifactory/libs-release-local</url>
                </repository>
                <snapshotRepository>
                    <id>snapshots</id>
                    <name>Artifactory Snapshots</name>
                    <url>https://artifactory.example.com/artifactory/libs-snapshot-local</url>
                </snapshotRepository>
            </distributionManagement>
        </profile>
    </profiles>
</project>
